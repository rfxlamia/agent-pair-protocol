{
  "name": "@agentpair/relay-conformance",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.typecheck.json --noEmit",
    "test": "vitest run --config vitest.config.ts",
    "conformance": "node --experimental-strip-types --no-warnings=ExperimentalWarning src/cli.ts"
  },
  "dependencies": {
    "@agentpair/protocol": "workspace:*",
    "@agentpair/relay": "workspace:*",
    "@noble/ciphers": "^2.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
